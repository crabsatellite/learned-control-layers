#ifndef _PMS_H_
#define _PMS_H_

// pms.h is included by build.h which defines all the methods.
// This file just provides the forward declaration bridge.
// All method implementations are in build.h, heuristic.h, and pms.h itself is minimal.

#endif
